spec:
  template:
    spec:
      containers:
        - name: {{APP_NAME}}
          livenessProbe:
            httpGet:
              path: /
              port: {{PORT}}
            initialDelaySeconds: 10
            periodSeconds: 5
