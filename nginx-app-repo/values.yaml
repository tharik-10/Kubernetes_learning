application:
  name: nginx-demo
  namespace: default
  image: nginx:1.25

replicas: 2

resources:
  cpu:
    request: "100m"
    limit: "500m"
  memory:
    request: "128Mi"
    limit: "256Mi"

features:
  livenessProbe: true
  readinessProbe: false

service:
  port: 80
